// Простий скрипт для додавання товару в кошик (симуляція)
document.querySelectorAll('. add-to-cart').forEach(button => {
    button.addEventListener(' click', () => {
        alert('Товар додано до кошика');
    });
});